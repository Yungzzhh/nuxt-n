import { useNuxtApp } from '#app'

export default defineNuxtRouteMiddleware((to, from) => {
  // if (to.name === 'inq-id') {
  //   if (from && from.name === 'inq') {
  //     return navigateTo('/inq', { replace: true })
  //   } else {
  //     return true
  //   }
  // }
})